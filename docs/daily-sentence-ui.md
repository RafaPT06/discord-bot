# Daily sentence controls

Daily sentence DMs use a Meowz embed with two controls:

- **Copy phrase** sends the quote again inside a Discord code block so it can be copied easily.
- **New phrase** replaces the current card with the next phrase and persists the advanced sequence index.

Only the configured `DAILY_SENTENCE_USER_ID` (or `OWNER_ID` fallback) can use these controls.
